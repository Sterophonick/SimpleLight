Add the Grit (GBA Raster Image Transmogrifier) files to this directory and run the "Build Skin Files.bat" to generate a config.ini then run it again to build the skin.

Builds the Dark skin by default, edit the config to change the path to light